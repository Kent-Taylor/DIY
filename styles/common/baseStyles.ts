import { StyleSheet } from 'react-native';
import { primary } from '../colors';

export default StyleSheet.create({
    container: {
        width: "100%",
        height: "100%",
        backgroundColor: primary,
    },
    viewWithBottomTabBar: {
        marginBottom: 80,
    }
});